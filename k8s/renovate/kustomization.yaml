apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: renovate
resources:
  - namespace.yaml
  - configmap.yaml
  - cronjob.yaml
# Create the `renovate` Secret out of band (gitlab-token, artifactory-user, artifactory-token).
