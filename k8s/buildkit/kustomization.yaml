apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: buildkit
resources:
  - namespace.yaml
  - configmap.yaml
  - statefulset.yaml
  - service.yaml
  - networkpolicy.yaml
  # - certificates.yaml   # enable when cert-manager is installed; otherwise apply the Secrets from scripts/gen-buildkit-certs.sh
